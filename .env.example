# Copy this file to .env and fill values locally. Every value is intentionally
# empty so this tracked template cannot be mistaken for usable configuration.
APP_URL=
STORAGE_URL=
ADMIN_EMAIL=
POSTGRES_PASSWORD=
MINIO_PASSWORD=
JWT_SECRET=
